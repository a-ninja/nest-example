generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//model category {
//  id              Int         @id @default(autoincrement())
//  description     String
//  name            String
//  createdAt       DateTime    @default(now())
//}

model product {
  id              Int         @id @default(autoincrement())
  description     String
  name            String
  productId       String
  createdAt       DateTime    @default(now())
  manufacturerId  Int // relation scalar field  (used in the `@relation` attribute a
//  category        category @relation(fields: [categoryId], references: [id])
//  categoryId      Int // relation scalar field  (used in the `@relation` attribute a
}

//model manufacturer {
//  id              Int         @id @default(autoincrement())
//  name            String
//  description     String
//  createdAt       DateTime    @default(now())
//}
